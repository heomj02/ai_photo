# AI Photo 개발 지침

- 이 프로젝트는 홈 폴더의 `~/ai_photo`를 사용하는 독립 Flutter/Android 작업 공간이다.
- 제품·구조·단계는 관련 `docs/` 문서를 읽고, 실제 코드와 사용자의 명시적 요청을 우선한다.
- 요청한 기능만 작은 단위로 구현하고 기존 스타일과 설치된 의존성을 우선한다.
- 주요 구조·의존성·동작 변경은 먼저 사용자와 합의한다.
- 설치·빌드·실행 명령은 루트 `RUNBOOK.md`에서 관리한다. README는 책임과 구조만 설명한다.
- SDK와 캐시는 `.tooling/`에 두고 Git에서 제외한다. 비밀정보와 사용자 사진을 커밋하지 않는다.
- 코드 변경 후 포맷·정적 분석·관련 테스트와 가능한 Android 빌드를 확인한다.
- 변경 작업마다 `PROJECT_LOG.md`에 한 항목을 추가한다. 필드 순서는 변경내용, 이유, 수정파일, 일시다.
- 명시적인 작업 요청 없이 파일 변경·설치·실행하지 않는다.
